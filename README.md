# DeskAI Plugin Template

This is a plugin template for DeskAI (https://deskai.app).

This project uses TypeScript to provide type checking. The repo depends on the latest plugin API (deskai.d.ts) in TypeScript Definition format, so you need install dependencies first.